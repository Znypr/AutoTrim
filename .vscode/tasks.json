{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "sweep:s1",
      "type": "shell",
      "command": "python",
      "args": [
        "trim.py", "videos/sample.mp4",
        "--noise=-32", "--min", "0.4", "--pad", "0.12", "--minkeep", "0.25", "--tag", "s1"
      ]
    },
    {
      "label": "sweep:s2",
      "type": "shell",
      "command": "python",
      "args": [
        "trim.py", "videos/sample.mp4",
        "--noise=-35", "--min", "0.6", "--pad", "0.15", "--minkeep", "0.25", "--tag", "s2"
      ]
    },
    {
      "label": "sweep:s3",
      "type": "shell",
      "command": "python",
      "args": [
        "trim.py", "videos/sample.mp4",
        "--noise=-40", "--min", "0.8", "--pad", "0.18", "--minkeep", "0.25", "--tag", "s3"
      ]
    },
    {
      "label": "Sweep thresholds",
      "dependsOn": ["sweep:s1", "sweep:s2", "sweep:s3"],
      "dependsOrder": "sequence"
    }
  ]
}
